import React, { FC } from 'react';
import * as styles from './app.module.scss';
import Header from './components/Header';

export const App = () => {
  return (
    <main>
      <Header />
    </main>
  );
};
